# Interpretations

Interpretations are pending regeneration from corrected model artifacts.

Future interpretation should be based on:

- leakage-free feature subsets
- model metadata under `models/`
- SHAP or feature-importance outputs generated from the corrected pipeline
- validation reports proving the modeled dataset matches the intended double-perovskite scope

Do not reuse legacy feature rankings or SHAP conclusions.

