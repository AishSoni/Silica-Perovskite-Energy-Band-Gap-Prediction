# Results

Corrected results are pending regeneration.

Legacy results were removed from the active project because they were tied to an invalid dataset collection workflow and leakage-prone preprocessing paths. New results must be generated by:

```bash
python run_pipeline.py --clean --download --validate --featurize --feature-selection --task both
```

When regenerated, this section should be populated only from:

- `data/raw/dataset_manifest.json`
- `results/dataset_validation/validation_summary.json`
- `results/corrected_pipeline_results.json`
- saved prediction files under `results/predictions/`
- model metadata under `models/`

Do not manually reintroduce old sample counts, feature counts, or performance values.

